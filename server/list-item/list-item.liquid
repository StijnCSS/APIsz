
<li class="joke-card">
  <p>{{ joke.joke }}</p>
  <div class="choices">
  {% if joke.rating == 'good' %}  <p>👍</p>
  {% else %}  <p>👎</p>
  {% endif %}
  
{% comment %} Delete function dat bijhoudt welke indexed item je delete {% endcomment %}
  <form action="/delete" method="POST">
    <input type="hidden" name="index" value="{{ forloop.index0 }}">
    <button class="button" type="submit">❌ </button>
  </form>
  </div>
</li>
